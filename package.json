{
  "name": "semaforo-disponibilita",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check public/app.js && node --check public/admin/admin.js"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
